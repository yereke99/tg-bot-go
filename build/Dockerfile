# Используем официальный образ Go нужной версии
FROM golang:1.23.4

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем всё содержимое локальной директории в контейнер
COPY . .

# Загрузка и установка зависимостей
RUN go mod tidy

# Открываем порт (при необходимости)
EXPOSE 8080

# Команда запуска вашего бота
CMD ["go", "run", "cmd/main.go"]
